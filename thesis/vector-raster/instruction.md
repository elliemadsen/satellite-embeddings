Generate a side by side png that compares raster data types to vector. e.g. left : point, line, polygon vectorized, right: bitmap versions of this.

This is also to illustrate larger object/field debtate in the context of GIS, as well as embedding space.
